# Система верификации пользователей

## Описание

Система позволяет администраторам верифицировать пользователей через админ-панель. Неверифицированные пользователи видят уведомление в шапке приложения.

## Функции

### 1. Статус верификации пользователя
- Каждый пользователь при регистрации получает статус "Не верифицирован" ❌
- После верификации администратором статус меняется на "Верифицирован" ✅

### 2. Уведомление в интерфейсе
- Неверифицированные пользователи видят желтое предупреждение в шапке
- Информация о статусе отображается рядом с именем пользователя
- При верификации уведомление исчезает

### 3. Админ-панель
- Таблица со всеми пользователями
- Отображение статуса верификации каждого пользователя
- Кнопка "Верифицировать" для неверифицированных пользователей
- Отображение дат регистрации и верификации

### 4. Telegram уведомления
При верификации пользователя:
```
✅ Пользователь верифицирован!

👤 Пользователь: username
⏰ Время: только что
✔️ Статус: Верифицирован
```

## API Endpoints

### Получить информацию о текущем пользователе
```
GET /api/v1/auth/me
Authorization: Token YOUR_TOKEN
```

**Ответ:**
```json
{
  "id": 1,
  "username": "user123",
  "profile": {
    "is_verified": false,
    "verified_at": null
  }
}
```

### Получить список всех пользователей (только админ)
```
GET /api/v1/auth/users
Authorization: Token YOUR_TOKEN
```

**Ответ:**
```json
[
  {
    "id": 1,
    "username": "user123",
    "is_verified": false,
    "created_at": "2025-12-07T12:00:00Z",
    "verified_at": null
  }
]
```

### Верифицировать пользователя (только админ)
```
POST /api/v1/auth/users/{user_id}/verify
Authorization: Token YOUR_TOKEN
```

**Ответ:**
```json
{
  "message": "Пользователь user123 верифицирован",
  "is_verified": true
}
```

## Использование в интерфейсе

### Проверка при входе
При успешной авторизации, информация о верификации сохраняется в localStorage:
- `isVerified`: "true" или "false"

### Отображение уведомления
В компоненте `root.tsx` отображается банер:
```
⚠️ Ваш аккаунт ещё не верифицирован. Пожалуйста, дождитесь подтверждения администратора.
```

### Админ-панель
1. Перейдите на страницу `/admin`
2. Найдите пользователя в таблице "Пользователи"
3. Нажмите кнопку "Верифицировать"
4. Пользователь получит уведомление в Telegram
5. При следующем входе уведомление исчезнет

## Структура БД

### Модель UserProfile
```python
user: OneToOneField(User)              # Связь с пользователем
is_verified: CharField                 # Статус: 'not_verified' или 'verified'
created_at: DateTimeField              # Дата регистрации
verified_at: DateTimeField (nullable)  # Дата верификации
```

## Интеграция с Django Admin

Для управления верификацией через Django админ-панель:
1. Перейдите на `http://localhost:8000/admin/`
2. Найдите раздел "Профили пользователей"
3. Выберите профиль пользователя
4. Измените статус верификации на "Верифицирован"
5. Сохраните
6. Пользователь автоматически получит Telegram уведомление

## Технические детали

- Frontend: React Router v7, TypeScript
- Backend: Django REST Framework
- Уведомления: python-telegram-bot
- Авторизация: Token Authentication

## Возможные улучшения

- [ ] Email уведомления при верификации
- [ ] История изменений статуса
- [ ] Причины отклонения верификации
- [ ] Автоматическая верификация по определённым критериям
- [ ] Истечение срока верификации
